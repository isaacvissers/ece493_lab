export const DECISION_VALUES = Object.freeze({
  accept: 'accept',
  reject: 'reject',
});

export const DECISION_STATUSES = Object.freeze({
  accepted: 'accepted',
  rejected: 'rejected',
});

export const REQUIRED_REVIEW_COUNT = 3;

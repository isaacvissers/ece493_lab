# Quickstart - Store Payment Confirmation

## Prerequisites
- Node.js and npm available on the development machine.

## Run tests
```bash
npm test
npm run lint
```

## Local usage notes
- This feature stores data in browser localStorage with an in-memory cache.
- Confirmations can be ingested via redirect or webhook-style handlers in the app.
